p#totalPosts You have {{posts.length}} posts in total:
div(ng-repeat='post in posts')#post
  h3 {{post.title}}
  div(ng-show="!post.hide")#text {{post.text}}
    a(href='/readPost/{{post.id}}') More
    |  - 
    a(href='/editPost/{{post.id}}') Edit
    |  - 
    a(href='/deletePost/{{post.id}}') Delete
  div(ng-show="post.hide")#text.textView 该内容已被管理员隐藏...
    a(href='/readPost/{{post.id}}') More
    |  - 
    a(href='/editPost/{{post.id}}') Edit
    |  - 
    a(href='/deletePost/{{post.id}}') Delete
  hr